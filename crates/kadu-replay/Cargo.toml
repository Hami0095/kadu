[package]
name = "kadu-replay"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
kadu-core = { path = "../kadu-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[features]
trace-hashes = ["kadu-core/trace-hashes"]
