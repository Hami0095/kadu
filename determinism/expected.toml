# The aggregate chain hash of `kadu bench --matches 10000 --seed 1`.
# This value changes ONLY when simulation behaviour changes on purpose.
# Updating it without a matching ruleset or engine version bump is a bug.
engine_version      = "0.2.0"
ruleset             = "2026.4"
ruleset_hash        = "0x974348015e70bac1"
bench_matches       = 10000
bench_seed          = 1
# Unchanged by Change 3 (passivity retune): kadu bench's fixed Rusher-vs-
# Dummy matchup never triggers passivity in the first place (Rusher stays
# the leader by continuously advancing and attacking, never idling), so
# this specific bench pairing is genuinely insensitive to the threshold -
# confirmed by rerunning the bench before and after and getting the same
# value. ruleset_hash above still changed, since the ruleset content did.
expected_aggregate  = "0x8bf709d03f3d7990"
